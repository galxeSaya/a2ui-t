# A2UI Playground (React + Tailwind)

## 运行

```bash
npm install
npm run dev
```

## 功能

- 3 个路由：`/`、`/playground`、`/about`
- Playground：输入 A2UI Surface JSON，右侧实时渲染

## 说明

- 官网文档里提到的 `@a2ui/web-lib` 目前尚未发布到 NPM（会 404），本项目使用已发布的 `@a2ui/lit/ui` 来渲染 Web Components（如 `<a2ui-surface>`）。

